<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RemotePCAgent" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Dipakai ScreenCaptureActivity: tak berujud, cuma memicu dialog sistem. -->
    <style name="Theme.Transparent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
